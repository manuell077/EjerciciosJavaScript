let numero = parseInt(prompt("Ingresa un numero entero")); // Pedimos al usuario que ingrese un numero ya sea par o impar


if(numero % 2 === 0){ //Validamos si el numero es par

 
    alert("EL numero es par "); //Imprimimos el numero par
}else{

    alert("El numero es impar"); }//Caso contario que imprima que no es par